# Users App

## Purpose
Manages user authentication, registration, and profiles.

## Key Components
- **Models**: `User` (Custom user model).
- **Views**: `RegisterView`, `UserProfileView`.
- **Auth**: Uses `simplejwt` for token-based authentication.
